{
    IBClasses = (
        {
            ACTIONS = {burn = id; setCDName = id; setup = id; }; 
            CLASS = BurnerWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                burnButton = NSButton; 
                compressionMode = NSMatrix; 
                nameField = NSTextField; 
                sizeField = NSTextField; 
                statusField = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}