{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {abortButton = id; }; 
            CLASS = Wait; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                abort = NSButton; 
                elapsed = NSTextField; 
                progress = NSProgressIndicator; 
                text = NSTextField; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}