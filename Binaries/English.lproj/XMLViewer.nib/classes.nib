{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                collapseAllItems = id; 
                deepCollapseAllItems = id; 
                deepExpandAllItems = id; 
                expandAllItems = id; 
                setSearchString = id; 
            }; 
            CLASS = XMLController; 
            LANGUAGE = ObjC; 
            OUTLETS = {search = NSSearchField; searchView = NSView; table = NSOutlineView; }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}