{
    IBClasses = (
        {
            ACTIONS = {"" = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                addDICOMField = id; 
                collapseAllItems = id; 
                deepCollapseAllItems = id; 
                deepExpandAllItems = id; 
                expandAllItems = id; 
                setSearchString = id; 
                switchDICOMEditing = id; 
            }; 
            CLASS = XMLController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                dicomEditingView = NSView; 
                levelMatrix = NSMatrix; 
                levelSelection = NSWindow; 
                search = NSSearchField; 
                searchView = NSView; 
                table = NSOutlineView; 
                tableScrollView = NSScrollView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}